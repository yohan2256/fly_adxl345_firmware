# Fly-ADXL345-USB 통신 프로토콜

RP2040이 USB CDC(가상 시리얼 포트)로 ADXL345 가속도계 데이터를 스트리밍하는
바이너리 프로토콜 명세. 호스트(PC/모바일 앱) 구현 시 이 문서를 기준으로 파싱기를
작성하면 된다.

## 1. 개요

- 전송 매체: USB CDC-ACM (가상 COM 포트), 펌웨어 → 호스트 방향이 기본(스트리밍),
  호스트 → 펌웨어 방향은 재전송 요청 커맨드만 존재.
- 센서: ADXL345, ODR 설정값 3200 Hz, FULL_RES 모드, ±16g, STREAM FIFO (watermark 16).
  단, ADXL345의 ODR은 내부 RC 오실레이터 기준이라 개체별로 최대 ±5%(상온 ±2%)
  벗어날 수 있다 — 3200 Hz는 명목값이며, 실제 값은 4b절의 상태 프레임으로 별도
  보고된다. 정밀 계산(고유주파수 기반 물성치 등)에는 상태 프레임의 실측값을
  써야 한다.
- 데이터는 **프레임(frame)** 단위로 전송되며, 두 가지 타입이 있다: 샘플을 담는
  **데이터 프레임**(2절)과 실측 ODR을 담는 **상태 프레임**(4b절). 데이터 프레임
  하나에 최대 32개 샘플이 담긴다.
- 모든 멀티바이트 필드는 **리틀 엔디안**.
- 호스트가 USB 포트를 열지 않은 상태(`CDC not connected`)에서는 펌웨어가 센서
  폴링 자체를 멈춘다 — 포트를 연 시점부터 스트리밍이 시작된다.

## 2. 프레임 포맷

```
offset  size  field        설명
0       1     sync1        고정값 0xFA
1       1     sync2        고정값 0xCE
2       2     seq_start    이 프레임의 첫 샘플 시퀀스 번호 (uint16, LE, 0xFFFF에서 0으로 랩어라운드)
4       1     count        이 프레임에 담긴 샘플 개수 (1~32)
5       1     flags        비트 플래그 (아래 3절 참조)
6       2     dropped      직전 보고 이후 유실된 샘플 수 (uint16, LE, 0xFFFF에서 포화)
8       6*N   samples[N]   가속도 샘플 N개, N = count (아래 4절 참조)
8+6N    2     crc16        헤더+샘플 전체에 대한 CRC16-CCITT (uint16, LE, 아래 5절 참조)
```

프레임 전체 길이 = `10 + 6*count` 바이트 (헤더 8B + CRC 2B + 샘플 6B×count).
`count`가 32일 때 최대 프레임 길이는 202바이트.

호스트는 스트림에서 `0xFA`를 찾은 뒤 다음 바이트로 프레임 타입을 구분한다:
`0xCE`이면 데이터 프레임(위 포맷, `count`로 길이 계산), `0xCD`이면 상태
프레임(4b절, 고정 8바이트 길이).

## 3. flags 비트

| 비트 | 이름 | 의미 |
|---|---|---|
| bit0 | `OVERRUN` | 이 프레임을 만들기 직전, ADXL345 하드웨어 FIFO에서 실제 오버런이 발생함 (INT_SOURCE 레지스터 기준). FIFO가 32개를 넘겨 오래된 샘플을 덮어썼다는 뜻 — 그 구간의 원본 샘플은 하드웨어 단에서 이미 사라졌으므로 재전송으로 복구 불가능. |
| bit1 | `DROPPED` | `dropped` 필드가 0이 아님을 알리는 보조 플래그 (링버퍼 과부하로 소프트웨어 단에서 프레임이 유실됨). |
| bit2 | `LINK_FAULT` | 주기적 DEVID 재확인에 실패함 (SPI 링크 이상 의심). 펌웨어가 즉시 센서를 재설정(reconfigure)하여 복구를 시도한 뒤 이 플래그를 세운 프레임을 보낸다. |
| bit3-7 | 예약 | 항상 0, 향후 확장용. |

## 4. 샘플 포맷

각 샘플은 6바이트, X/Y/Z 각각 `int16` (LE):

```
offset  size  field
0       2     x
2       2     y
4       2     z
```

FULL_RES ±16g 모드이므로 분해능은 range와 무관하게 고정 **~3.9 mg/LSB**이다.
물리 단위(g) 환산: `accel_g = raw * 0.0039`.

## 4b. 상태 프레임 (실측 ODR)

ADXL345의 ODR은 내부 RC 오실레이터에서 나오며, Analog Devices 공식 답변
기준 상온에서 ±2%, 전 온도 범위(-40~85°C)에서 최대 ±5% 벗어날 수 있다.
`FS_DEFAULT`(예: 3200.0 Hz)를 정확한 값으로 가정하면, 고유주파수 기반 물성치
계산처럼 `∝ fs²`로 스케일되는 값에서 오차가 제곱으로 증폭된다(±5% 클럭 오차
→ ±10% 물성치 오차). 이 오차는 랜덤이 아니라 기기별로 고정된 계통오차라
여러 대를 배치하면 기기끼리 값이 어긋난다.

이를 보정하기 위해 RP2040은 자체 크리스털(오차 수십 ppm 수준, ADXL345 내부
오실레이터보다 두 자릿수 이상 정확함) 기준으로 실제 FIFO에서 팝된 샘플 수를
약 1초 창마다 세어 실측 ODR을 계산하고, 아래 형식의 상태 프레임으로 약 1초
주기로 보고한다. 온도가 바뀌면 실측값도 따라 움직이므로 온도 드리프트까지
자동으로 반영된다.

```
offset  size  field        설명
0       1     sync1        고정값 0xFA
1       1     sync2        고정값 0xCD
2       4     odr_mhz      실측 ODR, milli-Hz 단위 (Hz*1000, uint32, LE)
6       2     crc16        sync1~odr_mhz에 대한 CRC16-CCITT (uint16, LE, 5절 참조)
```

프레임 전체 길이는 고정 **8바이트**이며, `count`/`seq_start`/`dropped` 필드가
없다 — 재전송 대상이 아니다(1초 뒤 새 값이 어차피 갱신되므로 개별 상태
프레임의 유실은 무해하다). 호스트는 물리 단위 환산 시 `odr_mhz / 1000.0`을
Hz 단위 ODR로 사용하면 된다(직전에 수신한 값을 최신 실측치로 취급).

USB 미연결로 수집이 중단되었다가 재개되면 측정 창이 그 시점부터 다시
시작되므로, 재연결 직후 첫 상태 프레임은 정상적으로 최대 약 1초 지연될 수
있다.

## 5. CRC16 (프레임 무결성)

- 알고리즘: **CRC16-CCITT**
- 다항식: `0x1021`
- 초기값: `0xFFFF`
- 입력: MSB부터 처리, 최종 XOR 없음
- 적용 범위: 프레임의 `sync1`부터 마지막 샘플 바이트까지 (CRC 필드 자체는 제외)

```c
uint16_t crc16_ccitt(const uint8_t *data, size_t len) {
    uint16_t crc = 0xFFFF;
    for (size_t i = 0; i < len; i++) {
        crc ^= (uint16_t)data[i] << 8;
        for (int b = 0; b < 8; b++)
            crc = (crc & 0x8000) ? (uint16_t)((crc << 1) ^ 0x1021)
                                  : (uint16_t)(crc << 1);
    }
    return crc;
}
```

CRC 불일치는 SPI 구간에서 샘플 값 자체가 손상되었거나(펌웨어가 검증할 수 없는
유일한 구간), USB 전송 중 애플리케이션 버퍼 처리 오류를 의미한다. 이 경우
호스트는 6절의 재전송을 요청할 수 있다.

## 6. 시퀀스 번호와 유실 감지

- `seq_start`는 매 프레임마다 `count`만큼 증가한다 (드롭된 프레임도 포함해서
  증가하므로, 호스트는 `이전 seq_start + 이전 count == 이번 seq_start`가
  깨지면 그 구간에 유실이 있었음을 알 수 있다).
- `dropped` 필드는 그 갭의 정확한 샘플 수를 알려준다 (링버퍼 과부하로 인한
  소프트웨어 유실만 해당 — bit0 OVERRUN으로 인한 하드웨어 유실은 여기 포함되지 않음).
- uint16 특성상 65536개 샘플(3200Hz 기준 약 20초)마다 랩어라운드하므로,
  갭 계산 시 wraparound를 고려해야 한다.

## 7. 재전송 요청 (호스트 → 펌웨어)

호스트가 특정 프레임을 못 받았거나(시퀀스 갭) CRC 검증에 실패했을 때, 같은
CDC 연결로 아래 4바이트를 보내면 펌웨어가 그 프레임을 다시 보낸다.

```
offset  size  field    값
0       1     sync1    고정값 0xF0
1       1     sync2    고정값 0x0D
2       2     seq      요청할 프레임의 seq_start (uint16, LE)
```

동작 방식:
- 펌웨어는 최근 전송된 프레임을 약 16KB(최대 256개) 이력 버퍼에 보관한다.
  `seq`와 일치하는 프레임이 있으면 **실시간 스트림보다 우선하여** 즉시 재전송한다.
- 재전송되는 프레임은 원본과 완전히 동일한 포맷(2절)이며, 재전송임을 나타내는
  별도 표시는 없다 — 호스트는 `seq_start`가 요청한 값과 일치하는 프레임이
  "채워졌다"고 판단하면 된다.
- **요청한 프레임이 이미 이력 버퍼에서 밀려났거나(evicted) 애초에 존재하지
  않았던 seq인 경우, 펌웨어는 아무 응답도 하지 않는다.** 호스트는 적절한
  타임아웃(예: 수십~수백 ms) 후 재요청하거나 포기해야 한다.
- 재전송은 **이력 버퍼에 실제로 들어간 프레임만** 대상이 된다. 이력 버퍼에는
  링버퍼 푸시에 성공한 프레임만 기록되므로:
  - `flags` bit1(`DROPPED`)로 보고된 구간 — 링버퍼가 가득 차서 애초에 큐에
    들어가지도 못한 프레임 — 은 이력에 없다. 재전송을 요청해도 응답이 오지
    않으며, 이 구간은 영구 유실로 처리해야 한다.
  - `flags` bit0(`OVERRUN`) 구간도 마찬가지로 복구 불가능하다 — 하드웨어
    FIFO가 이미 그 샘플을 덮어썼기 때문에 펌웨어도 값을 갖고 있지 않다.
  - 재전송이 실제로 의미 있는 경우는 **CRC 검증 실패**처럼, 프레임이 한 번
    큐에는 들어갔지만 그 이후(전송 구간)에서 손상되거나 호스트가 놓친 경우뿐이다.

## 8. 호스트 구현 시 권장 알고리즘 (요약)

1. 수신 바이트 스트림에서 `0xFA` 탐색 후 다음 바이트로 분기.
   - `0xCD`이면 상태 프레임: 고정 8바이트 버퍼링 후 CRC16 검증, 성공 시
     `odr_mhz`를 최신 실측 ODR로 갱신(4b절). 실패 시 재동기화만 하고 무시
     (재전송 대상 아님).
   - `0xCE`가 아니고 `0xCD`도 아니면 탐색 재개.
2. (데이터 프레임, `0xCE`) `count` 읽어 프레임 전체 길이 계산, 그만큼 버퍼링.
3. CRC16 검증.
   - 실패 시: 다음 `0xFA 0xCE` 위치로 재동기화하고, 이 프레임의 `seq_start`로
     재전송 요청(7절) 전송 — 단, 이 프레임 자체가 큐에는 들어갔던 것이 확실한
     경우(직전 정상 수신된 헤더로 `seq_start`/`count`를 미리 알고 있었던 경우)에만
     의미가 있다. 헤더 자체가 깨져 `seq_start`를 알 수 없다면 재전송을 요청할 값이
     없으므로 그냥 재동기화만 하고 유실로 처리한다.
   - 성공 시: `seq_start` 연속성 확인 → 갭이 있고 `dropped`가 0이면(즉, 갭의
     원인이 링버퍼 드롭이 아니라면) 재전송을 시도해볼 수 있다. `dropped`가
     갭 크기와 일치한다면 그 구간은 애초에 큐에 들어간 적이 없으므로 재전송
     요청 없이 바로 영구 유실로 처리한다.
4. `flags` 확인 후 상위 레이어(로깅/시각화 등)에 반영.
5. 재전송 요청은 타임아웃을 두고, 응답이 없으면 그 구간은 영구 유실로 처리.

## 9. 부트로더 진입 명령 (호스트 → 펌웨어, 원격 펌웨어 업데이트)

호스트가 새 펌웨어로 업데이트하려 할 때, 사용자가 물리적으로 BOOTSEL
버튼을 누를 필요 없이 같은 CDC 연결로 아래 2바이트를 보내면 즉시
RP2040의 ROM USB 부트로더로 재부팅한다.

```
offset  size  field    값
0       1     sync1    고정값 0xF0
1       1     sync2    고정값 0xB0
```

- payload 없음, 응답/ACK 없음 — 명령을 받는 즉시 `reset_usb_boot(0, 0)`을
  호출하고 리턴하지 않는다. 호스트 입장에서는 CDC 연결이 곧바로 끊긴다.
- 재부팅 후 장치는 표준 RP2040 부트롬 USB 장치(VID `0x2E8A`, PID `0x0003`)로
  재열거되며, **PICOBOOT** 인터페이스(벤더 클래스, bulk IN/OUT)를 통해
  새 `.uf2`/`.bin` 이미지를 플래시에 쓸 수 있다. 이 인터페이스는 ROM에
  내장되어 있으므로 이 펌웨어의 상태와 무관하게 항상 동일하게 동작한다
  (`picotool`이 사용하는 것과 같은 표준 프로토콜 — RP2040 datasheet 2.8.1절
  참조).
- 플래싱이 끝나고 호스트가 `REBOOT`(PICOBOOT 명령 `0x02`)을 보내면 새
  펌웨어로 다시 부팅되어 이 CDC 스트리밍 프로토콜(1~8절)이 재개된다.
- sync1(`0xF0`)은 7절 재전송 요청과 공유하며 `sync2`로 구분한다
  (`0x0D`=재전송, `0xB0`=부트로더 진입). 데이터/상태 프레임의 sync1은
  `0xFA`로 겹치지 않는다.
- 이 명령 자체에는 인증이 없다 — 같은 CDC 포트에 쓸 수 있는 주체라면
  누구나 부트로더 진입을 트리거할 수 있다. 물리적으로 USB 케이블에
  접근 가능한 위협 모델과 동일한 수준으로 간주한다.
